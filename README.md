# ScrapperGateway

Gateway .NET que orquesta los scrapers y el scoring de anuncios del ecosistema **DealRadar**.

Recoge los anuncios de los microservicios scraper, los normaliza a un esquema común y alimenta la API `NewApi` y el frontend.

**Stack:** C# · .NET · RabbitMQ · MongoDB · AutoMapper
